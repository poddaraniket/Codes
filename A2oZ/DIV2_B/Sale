#include <bits/stdc++.h>

using namespace std;

int main() {
   int n,m,s=0,i;
   cin>>n>>m;
   int a[n];
   for(i=0;i<n;i++)
    {
      cin>>a[i];
    }
   sort(a,a+n);
   for(i=0;i<m;i++)
    {
      if(a[i]<0)
       {
         s+=(a[i]*-1);
       }
    }
  cout<<s<<endl;  
}