#include <bits/stdc++.h>

using namespace std;
 
int main()  
{
   int n,i,s=0,diff;
   cin>>n;
   int a[n];
   for(i=0;i<n;i++)
     {
        cin>>a[i];
     }
    s+=a[0]+1;
    for(i=0;i<n-1;i++)
     {
         diff=abs(a[i]-a[i+1]);
         s+=2+diff;
     }
    cout<<s<<endl; 
}