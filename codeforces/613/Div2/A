#include <bits/stdc++.h>

using namespace std;

int main() {
  int n,l=0,r=0,i;
  cin>>n;
  string s;
  cin>>s;
  for(i=0;i<n;i++)
   {
     if(s[i]=='L')
      l++;
     else
      r++; 
   }
  cout<<l+r+1<<endl; 
}